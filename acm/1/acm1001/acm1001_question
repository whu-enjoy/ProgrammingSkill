dcription
给出两个整数 a b ，计算 a + b 的值并输出。

Input
输入数据只有一行，包含两个用空格分隔的整数 a b ，0 <= a, b <= 10
注意:
1. 整数是使用空格分隔的，所以scanf的格式串【不能】使用 "%d,%d"，而应当是"%d %d"
2. 上述范围是用于确定代码需要处理的数据规模（例如本题用int即可，不需要用long），不需要额外写代码验证输入是否有效。
Output
输出 a + b 的值（不要输出任何多余的内容）。
Sample Input
1 2

Sample Output
3
